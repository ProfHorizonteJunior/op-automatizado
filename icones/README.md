# Ícones

Coloque aqui os ícones oficiais do **AWS Architecture Icons** (formato `.svg`).

Download: https://aws.amazon.com/architecture/icons/

O campo `icone` de cada serviço em `arquiteturas/*.yaml` deve apontar para um
arquivo desta pasta (ex.: `Amazon-Simple-Storage-Service.svg`).
